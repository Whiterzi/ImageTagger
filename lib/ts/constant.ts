export enum eventMessages {
  created = "created"
};

export enum contextTypes {
  page = 'page',
  selection ='selection',
  link = 'link',
  editable = 'editable',
  image = 'image',
  video = 'video',
  audio = 'audio'
}